{
  "name": "wandori-waitlist",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Waitlist landing page for Wandori — a travel content discovery app for iOS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
